import DataAccess.DAO.CursoDAO;
import DataAccess.DAO.SexoDAO;
import DataAccess.DAO.TutorDAO;
import DataAccess.DTO.CursoDTO;
import DataAccess.DTO.SexoDTO;
import DataAccess.DTO.TutorDTO;

public class App {
    public static void main(String[] args) throws Exception {
        //SexoDAO sexoDAO = new SexoDAO();
        //for (SexoDTO s : sexoDAO.readAll()) System.out.println(s.toString());

        //TutorDAO tutorDAO = new TutorDAO();
        //for (TutorDTO t : tutorDAO.readAll()) System.out.println(t.toString());

        //CursoDAO cursoDAO = new CursoDAO();
        //for (CursoDTO c : cursoDAO.readAll()) System.out.println(c.toString());
    }
}
